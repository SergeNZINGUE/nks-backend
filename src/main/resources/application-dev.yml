spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/nks_dev}
    username: ${DB_USERNAME:nks}
    password: ${DB_PASSWORD:123456}
  jpa:
    show-sql: true

logging:
  level:
    bf.laterrasse.nks: DEBUG
    org.hibernate.SQL: DEBUG
